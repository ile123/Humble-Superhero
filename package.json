{
  "name": "humble-hero",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "client": "cd humble-hero-client && npm install && npm run dev",
    "api": "cd humble-hero-api && npm install && npm run start:dev",
    "start": "concurrently \"npm run client\" \"npm run api\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "concurrently": "^9.1.2"
  }
}
